import React from 'react'
import {render} from 'react-dom' // это импорт метода render
import App from './components/App' // это импорт метода render

render(<App />, document.getElementById("container"));